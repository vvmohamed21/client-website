# PROJECT_MAP.md — AVETO (nail.site)

> خريطة المشروع المرجعية. حدّثها فور أي تعديل معماري.

## 1. نظرة عامة

موقع شركة **AVETO** (استشارات هندسة وإعمار) ثنائي اللغة (TR/EN)، بدون أي إطار عمل أو قاعدة بيانات:
Node.js صافي (`http`) يقدّم ملفات ثابتة + REST API صغير لإدارة المحتوى.

- **التشغيل:** `node server.js` → `http://localhost:3000`
- **الاختبارات:** `node tests/run-tests.js` (86 فحص انحدار)

## 2. خريطة الملفات

| الملف | الدور |
|---|---|
| `server.js` | خادم HTTP: ملفات ثابتة + API الأدمن/المحتوى + جلسات في الذاكرة |
| `index.html` | الرئيسية (`data-page="home"`) |
| `services.html` / `portfolio.html` / `sigorta.html` / `ai-brief.html` / `contact.html` | صفحات الموقع (كلها تحمّل `content.js` + `app.js`) |
| `admin-login.html` | تسجيل دخول الأدمن |
| `admin.html` + `admin.js` | لوحة التحكم: تعديل النصوص/الصور/المشاريع/بيانات الدخول |
| `app.js` | الترجمات (tr/en)، التنقل، السكرول، نماذج AI Brief والتواصل. يصدّر `window.translations` ويستدعي `initSiteContent()` آخراً |
| `content.js` | يجلب `/api/content` (أو localStorage عند الفشل) ويطبّق تعديلات النصوص/الصور على الصفحة |
| `styles.css` | نظام التصميم الكامل (ثيم فاتح) |
| `data/site-content.json` | **"قاعدة البيانات"**: النصوص والصور والمشاريع المعدّلة |
| `data/admin-auth.json` | بيانات دخول الأدمن (scrypt hash + salt) |
| `favicon.svg` | أيقونة الموقع: مربع متدرج كحلي→أزرق بحرف A سيرافي |
| `favicon.ico` / `favicon-32/192/512.png` / `apple-touch-icon.png` | نفس التصميم بالصيغ الثنائية — مولّدة من `tools/generate-icons.ps1` |
| `tools/generate-icons.ps1` | إعادة توليد كل الأيقونات من الهوية (GDI+) — `powershell -File tools\generate-icons.ps1` |
| `tests/run-tests.js` | مجموعة اختبارات الانحدار (Branding/Favicon/Icons/Pipeline/Auth/Content/Cache) |

## 3. نظام الأدمن (بلا قاعدة بيانات)

**الفكرة:** ملفان JSON بدل DB — المحتوى في `data/site-content.json` وبيانات الدخول في `data/admin-auth.json`.

- **الدخول:** `http://localhost:3000/admin-login.html`
- **بيانات الدخول الافتراضية:** `admin` / `Anka2026!` (قابلة للتغيير من تبويب "Ayarlar" باللوحة)
- **الأمان:** تشفير scrypt لكلمة المرور، جلسات في ذاكرة الخادم بكوكي `HttpOnly` (تنتهي بإيقاف الخادم)، حماية `/admin.html` بإعادة توجيه، و`/data/` محظور على الزوار
- **API:**
  - `POST /api/admin/login` · `POST /api/admin/logout` · `GET /api/admin/check` · `PUT /api/admin/credentials`
  - `GET /api/content` (عام) · `PUT /api/content` (يتطلب جلسة)
- **مسار التعديل:** لوحة الأدمن → `PUT /api/content` → ملف JSON → أي زائر يفتح الموقع → `content.js` يجلب التعديلات ويطبقها فوق الترجمات الافتراضية
- **إضافة المشاريع (منظّمة):** تبويب "Projeler" — كل مشروع نموذج ثابت من قسمين: *Proje bilgileri* (الترتيب، النوع*، العنوان*، الوصف) و*Görseller* (الصورة الرئيسية* بمعاينة حية + صور قبل/بعد اختيارية). الحفظ يمر على `validateProjectChanges()`: النوع والعنوان إلزاميان والصورة يجب أن تكون http(s) وإلا يُرفض الحفظ برسالة تحدد رقم المشروع. الترتيب تلقائي بالترقيم أو أزرار ↑/↓
- **عرض المشاريع:** صفحة portfolio تعرضها ديناميكياً من البيانات عبر `#projectGrid` (لا بطاقات ثابتة) — ما تضيفه باللوحة يظهر فوراً بنفس التنسيق. عند غياب أي بيانات (فتح بدون خادم) تُعرض `DEFAULT_PROJECTS` من content.js
- **وضع بلا خادم (fallback):** عند فتح الملفات مباشرة بدون Node، يعمل localStorage فقط (دخول offline افتراضي بنفس البيانات الافتراضية)

## 4. نظام التصميم (الهوية)

| العنصر | القيمة |
|---|---|
| الاسم | **AVETO** فقط — دون أي لواحق (تم توحيده في كل الصفحات والعناوين والتذييلات) |
| الأزرق الأساسي | `--primary: #0B1E36` (كحلي)، داكن `#081729`، فاتح `#3399FF` |
| الأخضر الثانوي | `--secondary: #4caf50` (حالات النجاح/خطوات AI) |
| الثيم | فاتح (`color-scheme: light`) — خلفيات `#ffffff`/`#f8f9fa`، حدود `#e0e0e0` |
| الخطوط | العناوين: Playfair Display · النصوص: DM Sans |
| الأيقونة | `favicon.svg` (متدرج كحلي→أزرق + حرف A أبيض) — مربوطة في كل الصفحات |

**قرارات تناسق مثبتة بالاختبارات:** هامبرغر بلون primary، قائمة الموبايل بخلفية بيضاء، بطاقات المشاريع بيضاء، eyebrow فوق الخلفيات الداكنة بلون `--primary-light`.

## 5. سجل التعديلات

### 2026-08-21 (جلسة 3) — تحسين تجربة الهاتف
- `svh` مع بديل `vh` قبله في كل الارتفاعات (heroes) — متصفحات iOS/Android القديمة تتجاهل svh فتنهار الأقسام
- `text-size-adjust: 100%` على html — يمنع iOS من تكبير النص تلقائياً بالوضع الأفقي
- قائمة الجوال: ارتفاع أقصى `calc(100vh - 76px)` مع تمرير + روابط بمسحة لمس 12px
- `min-height: 240px` لسليدر قبل/بعد — حماية للمتصفحات دون دعم aspect-ratio
- `<meta name="theme-color" content="#0B1E36">` في كل الصفحات — شريط متصفح الهاتف بلون الهوية
- حقول الإدخال في صفحة الدخول واللوحة أصبحت 16px — يمنع iOS من تكبير الشاشة عند لمس الحقل
- زر الخروج في اللوحة يصبح ثابتاً بالتدفق على الشاشات ≤768px (كان يتراكب مع العنوان)

### 2026-08-21 (جلسة 2) — أيقونة بلا وميض + مشاريع ديناميكية منظمة
- **وميض الأيقونة القديمة عند التنقل:** السبب كان المتصفح يرجع تلقائياً لـ `/favicon.ico` والملفات القديمة. الحل: توليد نفس تصميم SVG بصيغ ثنائية (`tools/generate-icons.ps1` عبر GDI+) واستبدال `favicon.ico` وPNGs القديمة نهائياً، مع سلسلة روابط كاملة في كل صفحة (ico → svg → png-192 → apple-touch)
- **سياسة كاش:** `server.js` يرسل `Cache-Control: no-cache` لكل الملفات — لا صفحات ولا أيقونات قديمة تُقدَّم من الكاش بعد ذلك
- **المشاريع أصبحت ديناميكية:** حذف البطاقات الثابتة من portfolio.html واستبدالها بحاوية `#projectGrid` يغذيها `renderProjects()` من البيانات — مشاريع اللوحة تظهر فعلاً الآن
- **نموذج إضافة منظم:** قسمَي "Proje bilgileri / Görseller" بعناوين ومعاينة حية للصور، وتحقق إلزامي قبل الحفظ (`validateProjectChanges`)
- حذف مفاتيح الترجمة اليتيمة project1..3 من app.js، وإضافة `.empty-projects` و`.editor-group-title` للـ CSS

### 2026-08-21 — جراحة الهوية والأدمن والتناسق
- توحيد الاسم إلى **AVETO** في: عناوين الصفحات، hero الرئيسية، التذييلات، aria-labels، صفحات الأدمن
- إنشاء `favicon.svg` واستبدال رابط `imgn09.jpeg` في جميع الصفحات السبع
- **إصلاح خط أنابيب الأدمن (كان معطلاً بالكامل):**
  - `app.js`: إضافة `window.translations = translations` واستدعاء `window.initSiteContent?.()`
  - `content.js`: دمج التعديلات في قاموسَي tr وen (تبديل اللغة لا يفقد التعديلات)
  - إضافة `<script src="content.js">` الناقصة في services/portfolio/ai-brief
- CSS: `color-scheme: light`، لون الهامبرغر، خلفية قائمة الموبايل، خلفية بطاقات المشاريع، تباين eyebrow على الداكن، خلفية صفحة الدخول `#f8f9fa`
- استبدال شعار صفحة الدخول بعنصر `.brand-mark` الموجود (وحذف قاعدة `.login-header img` اليتيمة)
- إضافة `tests/run-tests.js`

## 6. نواقص معروفة (ديون)

- الجلسات في الذاكرة: إعادة تشغيل الخادم تُسجّل الجميع خروجاً (مقبول لحجم المشروع)
- بريد التواصل ما زال `engineer@ankabuild.studio` (بيانات وظيفية قابلة للتعديل من لوحة الأدمن — لم تُغيَّر لتجنّب اختلاق بيانات)
- ملفات الأيقونات الثنائية مولّدة آلياً من `tools/generate-icons.ps1` — أي تعديل مستقبلي على الهوية يتطلب إعادة تشغيل السكربت فقط
- وضع offline في `admin-login.html` يحتفظ ببيانات دخول افتراضية داخل JS العميل (لا يُستخدم عندما يعمل server.js)
